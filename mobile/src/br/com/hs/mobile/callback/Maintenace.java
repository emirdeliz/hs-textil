package br.com.hs.mobile.callback;

public interface Maintenace {
	void editar(Object entidade);
	void cancelar(Object entidade);
	void confirmar(Object entidade);
	void remover(Object entidade);
}
